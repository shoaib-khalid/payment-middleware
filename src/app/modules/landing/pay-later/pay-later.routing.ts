import { Route } from '@angular/router';
import { PayLaterComponent } from './pay-later.component';

export const payLaterRoutes: Route[] = [
    {
        path     : '',
        component: PayLaterComponent
    }
];
